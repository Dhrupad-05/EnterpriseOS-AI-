# ADR 001: Event-driven business backbone

Every request enters as a Business Event. One graph routes all event types, allowing new event types to be registered as data and agent capabilities rather than duplicated department workflows.
